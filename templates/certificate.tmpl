<!DOCTYPE html>
<html lang="{{.Lang}}">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="https://fonts.googleapis.com/css?family=Zilla+Slab" rel="stylesheet">
    <style>
        @page {
            size: A4 portrait;
        }
        body {
            width: 210mm;
            height: 297mm;
            margin: auto;
            background-color: #222222;
            font-family: 'Zilla Slab', serif;
            text-align: center;
        }
        body div,
        body h1 {
            margin: 0 auto;
            font-weight: 400;
        }       
        body h1 {
            color: #c4bb45;
            font-size: 88pt;
            padding: 36mm 0;
        }
        body .awarded-to,
        body .score {
            color: #d2d2d2;
            padding-bottom: 25mm;
            font-size: 33pt;
            font-variant: small-caps;
        }       
        body .email {
            color: #c4bb45;
            font-size: 42pt;
            padding-bottom: 25mm;
        }
        body .points {
            color: #c4bb45;
            font-size: 60pt;
            font-variant: normal;
        }
        body .iia {
            color: #d2d2d2;
            margin: auto;
            padding-top: 15mm;
            font-size: 21pt;
        }
    </style>
</head>
<body>
    <h1>{{.i18n.Tr "certificate"}}</h1>
    <div class="awarded-to">{{.i18n.Tr "awarded_to"}}</div> 
    <div class="email">{{.Email}}</div>
    <div class="score">
        {{.i18n.Tr "certificate_score_pre"}} <span class="points">{{.Score}}</span> {{.i18n.Tr "certificate_score_post"}}
    </div>
    <div class="iia">{{.i18n.Tr "service"}}</div>
</body>
</html>